<?php

/**
 * Base class that represents a row from the 'municipio' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Fri Jul 30 14:41:24 2010
 *
 * @package    lib.model.om
 */
abstract class BaseMunicipio extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        MunicipioPeer
	 */
	protected static $peer;

	/**
	 * The value for the mun_id field.
	 * @var        int
	 */
	protected $mun_id;

	/**
	 * The value for the mun_dep_id field.
	 * @var        int
	 */
	protected $mun_dep_id;

	/**
	 * The value for the mun_nombre field.
	 * @var        string
	 */
	protected $mun_nombre;

	/**
	 * The value for the mun_zona field.
	 * @var        string
	 */
	protected $mun_zona;

	/**
	 * @var        Departamento
	 */
	protected $aDepartamento;

	/**
	 * @var        array Disposicionfinalresiduosaseo[] Collection to store aggregation of Disposicionfinalresiduosaseo objects.
	 */
	protected $collDisposicionfinalresiduosaseos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDisposicionfinalresiduosaseos.
	 */
	private $lastDisposicionfinalresiduosaseoCriteria = null;

	/**
	 * @var        array Informaciongeneralmicrocuencas[] Collection to store aggregation of Informaciongeneralmicrocuencas objects.
	 */
	protected $collInformaciongeneralmicrocuencass;

	/**
	 * @var        Criteria The criteria used to select the current contents of collInformaciongeneralmicrocuencass.
	 */
	private $lastInformaciongeneralmicrocuencasCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'MunicipioPeer';

	/**
	 * Get the [mun_id] column value.
	 * 
	 * @return     int
	 */
	public function getMunId()
	{
		return $this->mun_id;
	}

	/**
	 * Get the [mun_dep_id] column value.
	 * 
	 * @return     int
	 */
	public function getMunDepId()
	{
		return $this->mun_dep_id;
	}

	/**
	 * Get the [mun_nombre] column value.
	 * 
	 * @return     string
	 */
	public function getMunNombre()
	{
		return $this->mun_nombre;
	}

	/**
	 * Get the [mun_zona] column value.
	 * 
	 * @return     string
	 */
	public function getMunZona()
	{
		return $this->mun_zona;
	}

	/**
	 * Set the value of [mun_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Municipio The current object (for fluent API support)
	 */
	public function setMunId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mun_id !== $v) {
			$this->mun_id = $v;
			$this->modifiedColumns[] = MunicipioPeer::MUN_ID;
		}

		return $this;
	} // setMunId()

	/**
	 * Set the value of [mun_dep_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Municipio The current object (for fluent API support)
	 */
	public function setMunDepId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mun_dep_id !== $v) {
			$this->mun_dep_id = $v;
			$this->modifiedColumns[] = MunicipioPeer::MUN_DEP_ID;
		}

		if ($this->aDepartamento !== null && $this->aDepartamento->getDepId() !== $v) {
			$this->aDepartamento = null;
		}

		return $this;
	} // setMunDepId()

	/**
	 * Set the value of [mun_nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     Municipio The current object (for fluent API support)
	 */
	public function setMunNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->mun_nombre !== $v) {
			$this->mun_nombre = $v;
			$this->modifiedColumns[] = MunicipioPeer::MUN_NOMBRE;
		}

		return $this;
	} // setMunNombre()

	/**
	 * Set the value of [mun_zona] column.
	 * 
	 * @param      string $v new value
	 * @return     Municipio The current object (for fluent API support)
	 */
	public function setMunZona($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->mun_zona !== $v) {
			$this->mun_zona = $v;
			$this->modifiedColumns[] = MunicipioPeer::MUN_ZONA;
		}

		return $this;
	} // setMunZona()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->mun_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->mun_dep_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->mun_nombre = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->mun_zona = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 4; // 4 = MunicipioPeer::NUM_COLUMNS - MunicipioPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Municipio object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aDepartamento !== null && $this->mun_dep_id !== $this->aDepartamento->getDepId()) {
			$this->aDepartamento = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MunicipioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = MunicipioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aDepartamento = null;
			$this->collDisposicionfinalresiduosaseos = null;
			$this->lastDisposicionfinalresiduosaseoCriteria = null;

			$this->collInformaciongeneralmicrocuencass = null;
			$this->lastInformaciongeneralmicrocuencasCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MunicipioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMunicipio:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				MunicipioPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMunicipio:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MunicipioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMunicipio:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMunicipio:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				MunicipioPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDepartamento !== null) {
				if ($this->aDepartamento->isModified() || $this->aDepartamento->isNew()) {
					$affectedRows += $this->aDepartamento->save($con);
				}
				$this->setDepartamento($this->aDepartamento);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = MunicipioPeer::MUN_ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = MunicipioPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setMunId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += MunicipioPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collDisposicionfinalresiduosaseos !== null) {
				foreach ($this->collDisposicionfinalresiduosaseos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collInformaciongeneralmicrocuencass !== null) {
				foreach ($this->collInformaciongeneralmicrocuencass as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDepartamento !== null) {
				if (!$this->aDepartamento->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDepartamento->getValidationFailures());
				}
			}


			if (($retval = MunicipioPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collDisposicionfinalresiduosaseos !== null) {
					foreach ($this->collDisposicionfinalresiduosaseos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collInformaciongeneralmicrocuencass !== null) {
					foreach ($this->collInformaciongeneralmicrocuencass as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MunicipioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getMunId();
				break;
			case 1:
				return $this->getMunDepId();
				break;
			case 2:
				return $this->getMunNombre();
				break;
			case 3:
				return $this->getMunZona();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = MunicipioPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getMunId(),
			$keys[1] => $this->getMunDepId(),
			$keys[2] => $this->getMunNombre(),
			$keys[3] => $this->getMunZona(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MunicipioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setMunId($value);
				break;
			case 1:
				$this->setMunDepId($value);
				break;
			case 2:
				$this->setMunNombre($value);
				break;
			case 3:
				$this->setMunZona($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = MunicipioPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setMunId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setMunDepId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setMunNombre($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setMunZona($arr[$keys[3]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);

		if ($this->isColumnModified(MunicipioPeer::MUN_ID)) $criteria->add(MunicipioPeer::MUN_ID, $this->mun_id);
		if ($this->isColumnModified(MunicipioPeer::MUN_DEP_ID)) $criteria->add(MunicipioPeer::MUN_DEP_ID, $this->mun_dep_id);
		if ($this->isColumnModified(MunicipioPeer::MUN_NOMBRE)) $criteria->add(MunicipioPeer::MUN_NOMBRE, $this->mun_nombre);
		if ($this->isColumnModified(MunicipioPeer::MUN_ZONA)) $criteria->add(MunicipioPeer::MUN_ZONA, $this->mun_zona);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);

		$criteria->add(MunicipioPeer::MUN_ID, $this->mun_id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getMunId();
	}

	/**
	 * Generic method to set the primary key (mun_id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setMunId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Municipio (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setMunDepId($this->mun_dep_id);

		$copyObj->setMunNombre($this->mun_nombre);

		$copyObj->setMunZona($this->mun_zona);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getDisposicionfinalresiduosaseos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDisposicionfinalresiduosaseo($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getInformaciongeneralmicrocuencass() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addInformaciongeneralmicrocuencas($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setMunId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Municipio Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     MunicipioPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new MunicipioPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Departamento object.
	 *
	 * @param      Departamento $v
	 * @return     Municipio The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDepartamento(Departamento $v = null)
	{
		if ($v === null) {
			$this->setMunDepId(NULL);
		} else {
			$this->setMunDepId($v->getDepId());
		}

		$this->aDepartamento = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Departamento object, it will not be re-added.
		if ($v !== null) {
			$v->addMunicipio($this);
		}

		return $this;
	}


	/**
	 * Get the associated Departamento object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Departamento The associated Departamento object.
	 * @throws     PropelException
	 */
	public function getDepartamento(PropelPDO $con = null)
	{
		if ($this->aDepartamento === null && ($this->mun_dep_id !== null)) {
			$this->aDepartamento = DepartamentoPeer::retrieveByPk($this->mun_dep_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDepartamento->addMunicipios($this);
			 */
		}
		return $this->aDepartamento;
	}

	/**
	 * Clears out the collDisposicionfinalresiduosaseos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDisposicionfinalresiduosaseos()
	 */
	public function clearDisposicionfinalresiduosaseos()
	{
		$this->collDisposicionfinalresiduosaseos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDisposicionfinalresiduosaseos collection (array).
	 *
	 * By default this just sets the collDisposicionfinalresiduosaseos collection to an empty array (like clearcollDisposicionfinalresiduosaseos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDisposicionfinalresiduosaseos()
	{
		$this->collDisposicionfinalresiduosaseos = array();
	}

	/**
	 * Gets an array of Disposicionfinalresiduosaseo objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Municipio has previously been saved, it will retrieve
	 * related Disposicionfinalresiduosaseos from storage. If this Municipio is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Disposicionfinalresiduosaseo[]
	 * @throws     PropelException
	 */
	public function getDisposicionfinalresiduosaseos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
			   $this->collDisposicionfinalresiduosaseos = array();
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				DisposicionfinalresiduosaseoPeer::addSelectColumns($criteria);
				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				DisposicionfinalresiduosaseoPeer::addSelectColumns($criteria);
				if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
					$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDisposicionfinalresiduosaseoCriteria = $criteria;
		return $this->collDisposicionfinalresiduosaseos;
	}

	/**
	 * Returns the number of related Disposicionfinalresiduosaseo objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Disposicionfinalresiduosaseo objects.
	 * @throws     PropelException
	 */
	public function countDisposicionfinalresiduosaseos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				$count = DisposicionfinalresiduosaseoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
					$count = DisposicionfinalresiduosaseoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDisposicionfinalresiduosaseos);
				}
			} else {
				$count = count($this->collDisposicionfinalresiduosaseos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Disposicionfinalresiduosaseo object to this object
	 * through the Disposicionfinalresiduosaseo foreign key attribute.
	 *
	 * @param      Disposicionfinalresiduosaseo $l Disposicionfinalresiduosaseo
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDisposicionfinalresiduosaseo(Disposicionfinalresiduosaseo $l)
	{
		if ($this->collDisposicionfinalresiduosaseos === null) {
			$this->initDisposicionfinalresiduosaseos();
		}
		if (!in_array($l, $this->collDisposicionfinalresiduosaseos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDisposicionfinalresiduosaseos, $l);
			$l->setMunicipio($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Disposicionfinalresiduosaseos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getDisposicionfinalresiduosaseosJoinAutoridadambiental($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
				$this->collDisposicionfinalresiduosaseos = array();
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinAutoridadambiental($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

			if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinAutoridadambiental($criteria, $con, $join_behavior);
			}
		}
		$this->lastDisposicionfinalresiduosaseoCriteria = $criteria;

		return $this->collDisposicionfinalresiduosaseos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Disposicionfinalresiduosaseos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getDisposicionfinalresiduosaseosJoinEstadogeneral($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
				$this->collDisposicionfinalresiduosaseos = array();
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinEstadogeneral($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

			if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinEstadogeneral($criteria, $con, $join_behavior);
			}
		}
		$this->lastDisposicionfinalresiduosaseoCriteria = $criteria;

		return $this->collDisposicionfinalresiduosaseos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Disposicionfinalresiduosaseos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getDisposicionfinalresiduosaseosJoinDepartamento($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
				$this->collDisposicionfinalresiduosaseos = array();
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

			if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		}
		$this->lastDisposicionfinalresiduosaseoCriteria = $criteria;

		return $this->collDisposicionfinalresiduosaseos;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Disposicionfinalresiduosaseos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getDisposicionfinalresiduosaseosJoinTecnicooperativo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDisposicionfinalresiduosaseos === null) {
			if ($this->isNew()) {
				$this->collDisposicionfinalresiduosaseos = array();
			} else {

				$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinTecnicooperativo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DisposicionfinalresiduosaseoPeer::DFR_MUNICIPIO_DISPOSICION_ID, $this->mun_id);

			if (!isset($this->lastDisposicionfinalresiduosaseoCriteria) || !$this->lastDisposicionfinalresiduosaseoCriteria->equals($criteria)) {
				$this->collDisposicionfinalresiduosaseos = DisposicionfinalresiduosaseoPeer::doSelectJoinTecnicooperativo($criteria, $con, $join_behavior);
			}
		}
		$this->lastDisposicionfinalresiduosaseoCriteria = $criteria;

		return $this->collDisposicionfinalresiduosaseos;
	}

	/**
	 * Clears out the collInformaciongeneralmicrocuencass collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addInformaciongeneralmicrocuencass()
	 */
	public function clearInformaciongeneralmicrocuencass()
	{
		$this->collInformaciongeneralmicrocuencass = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collInformaciongeneralmicrocuencass collection (array).
	 *
	 * By default this just sets the collInformaciongeneralmicrocuencass collection to an empty array (like clearcollInformaciongeneralmicrocuencass());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initInformaciongeneralmicrocuencass()
	{
		$this->collInformaciongeneralmicrocuencass = array();
	}

	/**
	 * Gets an array of Informaciongeneralmicrocuencas objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Municipio has previously been saved, it will retrieve
	 * related Informaciongeneralmicrocuencass from storage. If this Municipio is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Informaciongeneralmicrocuencas[]
	 * @throws     PropelException
	 */
	public function getInformaciongeneralmicrocuencass($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
			   $this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				InformaciongeneralmicrocuencasPeer::addSelectColumns($criteria);
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				InformaciongeneralmicrocuencasPeer::addSelectColumns($criteria);
				if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
					$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;
		return $this->collInformaciongeneralmicrocuencass;
	}

	/**
	 * Returns the number of related Informaciongeneralmicrocuencas objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Informaciongeneralmicrocuencas objects.
	 * @throws     PropelException
	 */
	public function countInformaciongeneralmicrocuencass(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				$count = InformaciongeneralmicrocuencasPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
					$count = InformaciongeneralmicrocuencasPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collInformaciongeneralmicrocuencass);
				}
			} else {
				$count = count($this->collInformaciongeneralmicrocuencass);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Informaciongeneralmicrocuencas object to this object
	 * through the Informaciongeneralmicrocuencas foreign key attribute.
	 *
	 * @param      Informaciongeneralmicrocuencas $l Informaciongeneralmicrocuencas
	 * @return     void
	 * @throws     PropelException
	 */
	public function addInformaciongeneralmicrocuencas(Informaciongeneralmicrocuencas $l)
	{
		if ($this->collInformaciongeneralmicrocuencass === null) {
			$this->initInformaciongeneralmicrocuencass();
		}
		if (!in_array($l, $this->collInformaciongeneralmicrocuencass, true)) { // only add it if the **same** object is not already associated
			array_push($this->collInformaciongeneralmicrocuencass, $l);
			$l->setMunicipio($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Informaciongeneralmicrocuencass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getInformaciongeneralmicrocuencassJoinMicrocuencas($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinMicrocuencas($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

			if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinMicrocuencas($criteria, $con, $join_behavior);
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;

		return $this->collInformaciongeneralmicrocuencass;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Municipio is new, it will return
	 * an empty collection; or if this Municipio has previously
	 * been saved, it will retrieve related Informaciongeneralmicrocuencass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Municipio.
	 */
	public function getInformaciongeneralmicrocuencassJoinDepartamento($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MunicipioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MUN_ID, $this->mun_id);

			if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;

		return $this->collInformaciongeneralmicrocuencass;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collDisposicionfinalresiduosaseos) {
				foreach ((array) $this->collDisposicionfinalresiduosaseos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collInformaciongeneralmicrocuencass) {
				foreach ((array) $this->collInformaciongeneralmicrocuencass as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collDisposicionfinalresiduosaseos = null;
		$this->collInformaciongeneralmicrocuencass = null;
			$this->aDepartamento = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseMunicipio:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseMunicipio::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseMunicipio
