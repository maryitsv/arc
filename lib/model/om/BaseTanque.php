<?php

/**
 * Base class that represents a row from the 'tanque' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * 07/29/10 00:07:06
 *
 * @package    lib.model.om
 */
abstract class BaseTanque extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        TanquePeer
	 */
	protected static $peer;

	/**
	 * The value for the tan_id field.
	 * @var        int
	 */
	protected $tan_id;

	/**
	 * The value for the tan_estado_id field.
	 * @var        int
	 */
	protected $tan_estado_id;

	/**
	 * The value for the tan_bypass_directo_red field.
	 * @var        string
	 */
	protected $tan_bypass_directo_red;

	/**
	 * The value for the tan_presencia_valvula_control field.
	 * @var        string
	 */
	protected $tan_presencia_valvula_control;

	/**
	 * The value for the tan_proteccion_tapa field.
	 * @var        string
	 */
	protected $tan_proteccion_tapa;

	/**
	 * The value for the tan_cerramiento_lote field.
	 * @var        string
	 */
	protected $tan_cerramiento_lote;

	/**
	 * The value for the tan_ventosa_salida field.
	 * @var        string
	 */
	protected $tan_ventosa_salida;

	/**
	 * The value for the tan_macro_medidor field.
	 * @var        string
	 */
	protected $tan_macro_medidor;

	/**
	 * The value for the tan_volumen field.
	 * @var        string
	 */
	protected $tan_volumen;

	/**
	 * @var        Estadogeneral
	 */
	protected $aEstadogeneral;

	/**
	 * @var        array Tecnicooperativaalmacenamientoacueducto[] Collection to store aggregation of Tecnicooperativaalmacenamientoacueducto objects.
	 */
	protected $collTecnicooperativaalmacenamientoacueductos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTecnicooperativaalmacenamientoacueductos.
	 */
	private $lastTecnicooperativaalmacenamientoacueductoCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'TanquePeer';

	/**
	 * Get the [tan_id] column value.
	 * 
	 * @return     int
	 */
	public function getTanId()
	{
		return $this->tan_id;
	}

	/**
	 * Get the [tan_estado_id] column value.
	 * 
	 * @return     int
	 */
	public function getTanEstadoId()
	{
		return $this->tan_estado_id;
	}

	/**
	 * Get the [tan_bypass_directo_red] column value.
	 * 
	 * @return     string
	 */
	public function getTanBypassDirectoRed()
	{
		return $this->tan_bypass_directo_red;
	}

	/**
	 * Get the [tan_presencia_valvula_control] column value.
	 * 
	 * @return     string
	 */
	public function getTanPresenciaValvulaControl()
	{
		return $this->tan_presencia_valvula_control;
	}

	/**
	 * Get the [tan_proteccion_tapa] column value.
	 * 
	 * @return     string
	 */
	public function getTanProteccionTapa()
	{
		return $this->tan_proteccion_tapa;
	}

	/**
	 * Get the [tan_cerramiento_lote] column value.
	 * 
	 * @return     string
	 */
	public function getTanCerramientoLote()
	{
		return $this->tan_cerramiento_lote;
	}

	/**
	 * Get the [tan_ventosa_salida] column value.
	 * 
	 * @return     string
	 */
	public function getTanVentosaSalida()
	{
		return $this->tan_ventosa_salida;
	}

	/**
	 * Get the [tan_macro_medidor] column value.
	 * 
	 * @return     string
	 */
	public function getTanMacroMedidor()
	{
		return $this->tan_macro_medidor;
	}

	/**
	 * Get the [tan_volumen] column value.
	 * 
	 * @return     string
	 */
	public function getTanVolumen()
	{
		return $this->tan_volumen;
	}

	/**
	 * Set the value of [tan_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->tan_id !== $v) {
			$this->tan_id = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_ID;
		}

		return $this;
	} // setTanId()

	/**
	 * Set the value of [tan_estado_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanEstadoId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->tan_estado_id !== $v) {
			$this->tan_estado_id = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_ESTADO_ID;
		}

		if ($this->aEstadogeneral !== null && $this->aEstadogeneral->getEsgId() !== $v) {
			$this->aEstadogeneral = null;
		}

		return $this;
	} // setTanEstadoId()

	/**
	 * Set the value of [tan_bypass_directo_red] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanBypassDirectoRed($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_bypass_directo_red !== $v) {
			$this->tan_bypass_directo_red = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_BYPASS_DIRECTO_RED;
		}

		return $this;
	} // setTanBypassDirectoRed()

	/**
	 * Set the value of [tan_presencia_valvula_control] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanPresenciaValvulaControl($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_presencia_valvula_control !== $v) {
			$this->tan_presencia_valvula_control = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_PRESENCIA_VALVULA_CONTROL;
		}

		return $this;
	} // setTanPresenciaValvulaControl()

	/**
	 * Set the value of [tan_proteccion_tapa] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanProteccionTapa($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_proteccion_tapa !== $v) {
			$this->tan_proteccion_tapa = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_PROTECCION_TAPA;
		}

		return $this;
	} // setTanProteccionTapa()

	/**
	 * Set the value of [tan_cerramiento_lote] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanCerramientoLote($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_cerramiento_lote !== $v) {
			$this->tan_cerramiento_lote = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_CERRAMIENTO_LOTE;
		}

		return $this;
	} // setTanCerramientoLote()

	/**
	 * Set the value of [tan_ventosa_salida] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanVentosaSalida($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_ventosa_salida !== $v) {
			$this->tan_ventosa_salida = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_VENTOSA_SALIDA;
		}

		return $this;
	} // setTanVentosaSalida()

	/**
	 * Set the value of [tan_macro_medidor] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanMacroMedidor($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_macro_medidor !== $v) {
			$this->tan_macro_medidor = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_MACRO_MEDIDOR;
		}

		return $this;
	} // setTanMacroMedidor()

	/**
	 * Set the value of [tan_volumen] column.
	 * 
	 * @param      string $v new value
	 * @return     Tanque The current object (for fluent API support)
	 */
	public function setTanVolumen($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tan_volumen !== $v) {
			$this->tan_volumen = $v;
			$this->modifiedColumns[] = TanquePeer::TAN_VOLUMEN;
		}

		return $this;
	} // setTanVolumen()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->tan_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->tan_estado_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->tan_bypass_directo_red = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->tan_presencia_valvula_control = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->tan_proteccion_tapa = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->tan_cerramiento_lote = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->tan_ventosa_salida = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->tan_macro_medidor = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->tan_volumen = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 9; // 9 = TanquePeer::NUM_COLUMNS - TanquePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Tanque object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aEstadogeneral !== null && $this->tan_estado_id !== $this->aEstadogeneral->getEsgId()) {
			$this->aEstadogeneral = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TanquePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = TanquePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aEstadogeneral = null;
			$this->collTecnicooperativaalmacenamientoacueductos = null;
			$this->lastTecnicooperativaalmacenamientoacueductoCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TanquePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTanque:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				TanquePeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTanque:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TanquePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTanque:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTanque:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				TanquePeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEstadogeneral !== null) {
				if ($this->aEstadogeneral->isModified() || $this->aEstadogeneral->isNew()) {
					$affectedRows += $this->aEstadogeneral->save($con);
				}
				$this->setEstadogeneral($this->aEstadogeneral);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = TanquePeer::TAN_ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = TanquePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setTanId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += TanquePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collTecnicooperativaalmacenamientoacueductos !== null) {
				foreach ($this->collTecnicooperativaalmacenamientoacueductos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEstadogeneral !== null) {
				if (!$this->aEstadogeneral->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aEstadogeneral->getValidationFailures());
				}
			}


			if (($retval = TanquePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collTecnicooperativaalmacenamientoacueductos !== null) {
					foreach ($this->collTecnicooperativaalmacenamientoacueductos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TanquePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getTanId();
				break;
			case 1:
				return $this->getTanEstadoId();
				break;
			case 2:
				return $this->getTanBypassDirectoRed();
				break;
			case 3:
				return $this->getTanPresenciaValvulaControl();
				break;
			case 4:
				return $this->getTanProteccionTapa();
				break;
			case 5:
				return $this->getTanCerramientoLote();
				break;
			case 6:
				return $this->getTanVentosaSalida();
				break;
			case 7:
				return $this->getTanMacroMedidor();
				break;
			case 8:
				return $this->getTanVolumen();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = TanquePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getTanId(),
			$keys[1] => $this->getTanEstadoId(),
			$keys[2] => $this->getTanBypassDirectoRed(),
			$keys[3] => $this->getTanPresenciaValvulaControl(),
			$keys[4] => $this->getTanProteccionTapa(),
			$keys[5] => $this->getTanCerramientoLote(),
			$keys[6] => $this->getTanVentosaSalida(),
			$keys[7] => $this->getTanMacroMedidor(),
			$keys[8] => $this->getTanVolumen(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TanquePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setTanId($value);
				break;
			case 1:
				$this->setTanEstadoId($value);
				break;
			case 2:
				$this->setTanBypassDirectoRed($value);
				break;
			case 3:
				$this->setTanPresenciaValvulaControl($value);
				break;
			case 4:
				$this->setTanProteccionTapa($value);
				break;
			case 5:
				$this->setTanCerramientoLote($value);
				break;
			case 6:
				$this->setTanVentosaSalida($value);
				break;
			case 7:
				$this->setTanMacroMedidor($value);
				break;
			case 8:
				$this->setTanVolumen($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = TanquePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setTanId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setTanEstadoId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTanBypassDirectoRed($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setTanPresenciaValvulaControl($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setTanProteccionTapa($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setTanCerramientoLote($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setTanVentosaSalida($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setTanMacroMedidor($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setTanVolumen($arr[$keys[8]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(TanquePeer::DATABASE_NAME);

		if ($this->isColumnModified(TanquePeer::TAN_ID)) $criteria->add(TanquePeer::TAN_ID, $this->tan_id);
		if ($this->isColumnModified(TanquePeer::TAN_ESTADO_ID)) $criteria->add(TanquePeer::TAN_ESTADO_ID, $this->tan_estado_id);
		if ($this->isColumnModified(TanquePeer::TAN_BYPASS_DIRECTO_RED)) $criteria->add(TanquePeer::TAN_BYPASS_DIRECTO_RED, $this->tan_bypass_directo_red);
		if ($this->isColumnModified(TanquePeer::TAN_PRESENCIA_VALVULA_CONTROL)) $criteria->add(TanquePeer::TAN_PRESENCIA_VALVULA_CONTROL, $this->tan_presencia_valvula_control);
		if ($this->isColumnModified(TanquePeer::TAN_PROTECCION_TAPA)) $criteria->add(TanquePeer::TAN_PROTECCION_TAPA, $this->tan_proteccion_tapa);
		if ($this->isColumnModified(TanquePeer::TAN_CERRAMIENTO_LOTE)) $criteria->add(TanquePeer::TAN_CERRAMIENTO_LOTE, $this->tan_cerramiento_lote);
		if ($this->isColumnModified(TanquePeer::TAN_VENTOSA_SALIDA)) $criteria->add(TanquePeer::TAN_VENTOSA_SALIDA, $this->tan_ventosa_salida);
		if ($this->isColumnModified(TanquePeer::TAN_MACRO_MEDIDOR)) $criteria->add(TanquePeer::TAN_MACRO_MEDIDOR, $this->tan_macro_medidor);
		if ($this->isColumnModified(TanquePeer::TAN_VOLUMEN)) $criteria->add(TanquePeer::TAN_VOLUMEN, $this->tan_volumen);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(TanquePeer::DATABASE_NAME);

		$criteria->add(TanquePeer::TAN_ID, $this->tan_id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getTanId();
	}

	/**
	 * Generic method to set the primary key (tan_id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setTanId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Tanque (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setTanEstadoId($this->tan_estado_id);

		$copyObj->setTanBypassDirectoRed($this->tan_bypass_directo_red);

		$copyObj->setTanPresenciaValvulaControl($this->tan_presencia_valvula_control);

		$copyObj->setTanProteccionTapa($this->tan_proteccion_tapa);

		$copyObj->setTanCerramientoLote($this->tan_cerramiento_lote);

		$copyObj->setTanVentosaSalida($this->tan_ventosa_salida);

		$copyObj->setTanMacroMedidor($this->tan_macro_medidor);

		$copyObj->setTanVolumen($this->tan_volumen);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getTecnicooperativaalmacenamientoacueductos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTecnicooperativaalmacenamientoacueducto($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setTanId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Tanque Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     TanquePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new TanquePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Estadogeneral object.
	 *
	 * @param      Estadogeneral $v
	 * @return     Tanque The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setEstadogeneral(Estadogeneral $v = null)
	{
		if ($v === null) {
			$this->setTanEstadoId(NULL);
		} else {
			$this->setTanEstadoId($v->getEsgId());
		}

		$this->aEstadogeneral = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Estadogeneral object, it will not be re-added.
		if ($v !== null) {
			$v->addTanque($this);
		}

		return $this;
	}


	/**
	 * Get the associated Estadogeneral object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Estadogeneral The associated Estadogeneral object.
	 * @throws     PropelException
	 */
	public function getEstadogeneral(PropelPDO $con = null)
	{
		if ($this->aEstadogeneral === null && ($this->tan_estado_id !== null)) {
			$this->aEstadogeneral = EstadogeneralPeer::retrieveByPk($this->tan_estado_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aEstadogeneral->addTanques($this);
			 */
		}
		return $this->aEstadogeneral;
	}

	/**
	 * Clears out the collTecnicooperativaalmacenamientoacueductos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTecnicooperativaalmacenamientoacueductos()
	 */
	public function clearTecnicooperativaalmacenamientoacueductos()
	{
		$this->collTecnicooperativaalmacenamientoacueductos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTecnicooperativaalmacenamientoacueductos collection (array).
	 *
	 * By default this just sets the collTecnicooperativaalmacenamientoacueductos collection to an empty array (like clearcollTecnicooperativaalmacenamientoacueductos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTecnicooperativaalmacenamientoacueductos()
	{
		$this->collTecnicooperativaalmacenamientoacueductos = array();
	}

	/**
	 * Gets an array of Tecnicooperativaalmacenamientoacueducto objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Tanque has previously been saved, it will retrieve
	 * related Tecnicooperativaalmacenamientoacueductos from storage. If this Tanque is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Tecnicooperativaalmacenamientoacueducto[]
	 * @throws     PropelException
	 */
	public function getTecnicooperativaalmacenamientoacueductos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TanquePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTecnicooperativaalmacenamientoacueductos === null) {
			if ($this->isNew()) {
			   $this->collTecnicooperativaalmacenamientoacueductos = array();
			} else {

				$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

				TecnicooperativaalmacenamientoacueductoPeer::addSelectColumns($criteria);
				$this->collTecnicooperativaalmacenamientoacueductos = TecnicooperativaalmacenamientoacueductoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

				TecnicooperativaalmacenamientoacueductoPeer::addSelectColumns($criteria);
				if (!isset($this->lastTecnicooperativaalmacenamientoacueductoCriteria) || !$this->lastTecnicooperativaalmacenamientoacueductoCriteria->equals($criteria)) {
					$this->collTecnicooperativaalmacenamientoacueductos = TecnicooperativaalmacenamientoacueductoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTecnicooperativaalmacenamientoacueductoCriteria = $criteria;
		return $this->collTecnicooperativaalmacenamientoacueductos;
	}

	/**
	 * Returns the number of related Tecnicooperativaalmacenamientoacueducto objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Tecnicooperativaalmacenamientoacueducto objects.
	 * @throws     PropelException
	 */
	public function countTecnicooperativaalmacenamientoacueductos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TanquePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTecnicooperativaalmacenamientoacueductos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

				$count = TecnicooperativaalmacenamientoacueductoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

				if (!isset($this->lastTecnicooperativaalmacenamientoacueductoCriteria) || !$this->lastTecnicooperativaalmacenamientoacueductoCriteria->equals($criteria)) {
					$count = TecnicooperativaalmacenamientoacueductoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTecnicooperativaalmacenamientoacueductos);
				}
			} else {
				$count = count($this->collTecnicooperativaalmacenamientoacueductos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Tecnicooperativaalmacenamientoacueducto object to this object
	 * through the Tecnicooperativaalmacenamientoacueducto foreign key attribute.
	 *
	 * @param      Tecnicooperativaalmacenamientoacueducto $l Tecnicooperativaalmacenamientoacueducto
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTecnicooperativaalmacenamientoacueducto(Tecnicooperativaalmacenamientoacueducto $l)
	{
		if ($this->collTecnicooperativaalmacenamientoacueductos === null) {
			$this->initTecnicooperativaalmacenamientoacueductos();
		}
		if (!in_array($l, $this->collTecnicooperativaalmacenamientoacueductos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTecnicooperativaalmacenamientoacueductos, $l);
			$l->setTanque($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Tanque is new, it will return
	 * an empty collection; or if this Tanque has previously
	 * been saved, it will retrieve related Tecnicooperativaalmacenamientoacueductos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Tanque.
	 */
	public function getTecnicooperativaalmacenamientoacueductosJoinTecnicooperativo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TanquePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTecnicooperativaalmacenamientoacueductos === null) {
			if ($this->isNew()) {
				$this->collTecnicooperativaalmacenamientoacueductos = array();
			} else {

				$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

				$this->collTecnicooperativaalmacenamientoacueductos = TecnicooperativaalmacenamientoacueductoPeer::doSelectJoinTecnicooperativo($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TecnicooperativaalmacenamientoacueductoPeer::TOAA_TAN_ID, $this->tan_id);

			if (!isset($this->lastTecnicooperativaalmacenamientoacueductoCriteria) || !$this->lastTecnicooperativaalmacenamientoacueductoCriteria->equals($criteria)) {
				$this->collTecnicooperativaalmacenamientoacueductos = TecnicooperativaalmacenamientoacueductoPeer::doSelectJoinTecnicooperativo($criteria, $con, $join_behavior);
			}
		}
		$this->lastTecnicooperativaalmacenamientoacueductoCriteria = $criteria;

		return $this->collTecnicooperativaalmacenamientoacueductos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collTecnicooperativaalmacenamientoacueductos) {
				foreach ((array) $this->collTecnicooperativaalmacenamientoacueductos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collTecnicooperativaalmacenamientoacueductos = null;
			$this->aEstadogeneral = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseTanque:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseTanque::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseTanque
