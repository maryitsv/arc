<?php

/**
 * Base class that represents a row from the 'microcuencas' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * 07/30/10 18:50:54
 *
 * @package    lib.model.om
 */
abstract class BaseMicrocuencas extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        MicrocuencasPeer
	 */
	protected static $peer;

	/**
	 * The value for the mic_id field.
	 * @var        int
	 */
	protected $mic_id;

	/**
	 * The value for the mic_pps_pre_id field.
	 * @var        int
	 */
	protected $mic_pps_pre_id;

	/**
	 * The value for the mic_pps_anio field.
	 * @var        int
	 */
	protected $mic_pps_anio;

	/**
	 * The value for the mic_pps_ser_id field.
	 * @var        int
	 */
	protected $mic_pps_ser_id;

	/**
	 * @var        Periodoporprestadorservicio
	 */
	protected $aPeriodoporprestadorservicio;

	/**
	 * @var        array Calidadcantidadagua[] Collection to store aggregation of Calidadcantidadagua objects.
	 */
	protected $collCalidadcantidadaguas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCalidadcantidadaguas.
	 */
	private $lastCalidadcantidadaguaCriteria = null;

	/**
	 * @var        array Fuentes[] Collection to store aggregation of Fuentes objects.
	 */
	protected $collFuentess;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFuentess.
	 */
	private $lastFuentesCriteria = null;

	/**
	 * @var        array Informaciongeneralmicrocuencas[] Collection to store aggregation of Informaciongeneralmicrocuencas objects.
	 */
	protected $collInformaciongeneralmicrocuencass;

	/**
	 * @var        Criteria The criteria used to select the current contents of collInformaciongeneralmicrocuencass.
	 */
	private $lastInformaciongeneralmicrocuencasCriteria = null;

	/**
	 * @var        array Proteccionfuentessuperficialesagua[] Collection to store aggregation of Proteccionfuentessuperficialesagua objects.
	 */
	protected $collProteccionfuentessuperficialesaguas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collProteccionfuentessuperficialesaguas.
	 */
	private $lastProteccionfuentessuperficialesaguaCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'MicrocuencasPeer';

	/**
	 * Get the [mic_id] column value.
	 * 
	 * @return     int
	 */
	public function getMicId()
	{
		return $this->mic_id;
	}

	/**
	 * Get the [mic_pps_pre_id] column value.
	 * 
	 * @return     int
	 */
	public function getMicPpsPreId()
	{
		return $this->mic_pps_pre_id;
	}

	/**
	 * Get the [mic_pps_anio] column value.
	 * 
	 * @return     int
	 */
	public function getMicPpsAnio()
	{
		return $this->mic_pps_anio;
	}

	/**
	 * Get the [mic_pps_ser_id] column value.
	 * 
	 * @return     int
	 */
	public function getMicPpsSerId()
	{
		return $this->mic_pps_ser_id;
	}

	/**
	 * Set the value of [mic_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Microcuencas The current object (for fluent API support)
	 */
	public function setMicId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mic_id !== $v) {
			$this->mic_id = $v;
			$this->modifiedColumns[] = MicrocuencasPeer::MIC_ID;
		}

		return $this;
	} // setMicId()

	/**
	 * Set the value of [mic_pps_pre_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Microcuencas The current object (for fluent API support)
	 */
	public function setMicPpsPreId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mic_pps_pre_id !== $v) {
			$this->mic_pps_pre_id = $v;
			$this->modifiedColumns[] = MicrocuencasPeer::MIC_PPS_PRE_ID;
		}

		if ($this->aPeriodoporprestadorservicio !== null && $this->aPeriodoporprestadorservicio->getPpsPreId() !== $v) {
			$this->aPeriodoporprestadorservicio = null;
		}

		return $this;
	} // setMicPpsPreId()

	/**
	 * Set the value of [mic_pps_anio] column.
	 * 
	 * @param      int $v new value
	 * @return     Microcuencas The current object (for fluent API support)
	 */
	public function setMicPpsAnio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mic_pps_anio !== $v) {
			$this->mic_pps_anio = $v;
			$this->modifiedColumns[] = MicrocuencasPeer::MIC_PPS_ANIO;
		}

		return $this;
	} // setMicPpsAnio()

	/**
	 * Set the value of [mic_pps_ser_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Microcuencas The current object (for fluent API support)
	 */
	public function setMicPpsSerId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->mic_pps_ser_id !== $v) {
			$this->mic_pps_ser_id = $v;
			$this->modifiedColumns[] = MicrocuencasPeer::MIC_PPS_SER_ID;
		}

		return $this;
	} // setMicPpsSerId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->mic_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->mic_pps_pre_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->mic_pps_anio = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->mic_pps_ser_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 4; // 4 = MicrocuencasPeer::NUM_COLUMNS - MicrocuencasPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Microcuencas object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aPeriodoporprestadorservicio !== null && $this->mic_pps_pre_id !== $this->aPeriodoporprestadorservicio->getPpsPreId()) {
			$this->aPeriodoporprestadorservicio = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MicrocuencasPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = MicrocuencasPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aPeriodoporprestadorservicio = null;
			$this->collCalidadcantidadaguas = null;
			$this->lastCalidadcantidadaguaCriteria = null;

			$this->collFuentess = null;
			$this->lastFuentesCriteria = null;

			$this->collInformaciongeneralmicrocuencass = null;
			$this->lastInformaciongeneralmicrocuencasCriteria = null;

			$this->collProteccionfuentessuperficialesaguas = null;
			$this->lastProteccionfuentessuperficialesaguaCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MicrocuencasPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMicrocuencas:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				MicrocuencasPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMicrocuencas:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MicrocuencasPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMicrocuencas:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMicrocuencas:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				MicrocuencasPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPeriodoporprestadorservicio !== null) {
				if ($this->aPeriodoporprestadorservicio->isModified() || $this->aPeriodoporprestadorservicio->isNew()) {
					$affectedRows += $this->aPeriodoporprestadorservicio->save($con);
				}
				$this->setPeriodoporprestadorservicio($this->aPeriodoporprestadorservicio);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = MicrocuencasPeer::MIC_ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = MicrocuencasPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setMicId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += MicrocuencasPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCalidadcantidadaguas !== null) {
				foreach ($this->collCalidadcantidadaguas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFuentess !== null) {
				foreach ($this->collFuentess as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collInformaciongeneralmicrocuencass !== null) {
				foreach ($this->collInformaciongeneralmicrocuencass as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collProteccionfuentessuperficialesaguas !== null) {
				foreach ($this->collProteccionfuentessuperficialesaguas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPeriodoporprestadorservicio !== null) {
				if (!$this->aPeriodoporprestadorservicio->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPeriodoporprestadorservicio->getValidationFailures());
				}
			}


			if (($retval = MicrocuencasPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCalidadcantidadaguas !== null) {
					foreach ($this->collCalidadcantidadaguas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFuentess !== null) {
					foreach ($this->collFuentess as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collInformaciongeneralmicrocuencass !== null) {
					foreach ($this->collInformaciongeneralmicrocuencass as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collProteccionfuentessuperficialesaguas !== null) {
					foreach ($this->collProteccionfuentessuperficialesaguas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MicrocuencasPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getMicId();
				break;
			case 1:
				return $this->getMicPpsPreId();
				break;
			case 2:
				return $this->getMicPpsAnio();
				break;
			case 3:
				return $this->getMicPpsSerId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = MicrocuencasPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getMicId(),
			$keys[1] => $this->getMicPpsPreId(),
			$keys[2] => $this->getMicPpsAnio(),
			$keys[3] => $this->getMicPpsSerId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MicrocuencasPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setMicId($value);
				break;
			case 1:
				$this->setMicPpsPreId($value);
				break;
			case 2:
				$this->setMicPpsAnio($value);
				break;
			case 3:
				$this->setMicPpsSerId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = MicrocuencasPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setMicId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setMicPpsPreId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setMicPpsAnio($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setMicPpsSerId($arr[$keys[3]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);

		if ($this->isColumnModified(MicrocuencasPeer::MIC_ID)) $criteria->add(MicrocuencasPeer::MIC_ID, $this->mic_id);
		if ($this->isColumnModified(MicrocuencasPeer::MIC_PPS_PRE_ID)) $criteria->add(MicrocuencasPeer::MIC_PPS_PRE_ID, $this->mic_pps_pre_id);
		if ($this->isColumnModified(MicrocuencasPeer::MIC_PPS_ANIO)) $criteria->add(MicrocuencasPeer::MIC_PPS_ANIO, $this->mic_pps_anio);
		if ($this->isColumnModified(MicrocuencasPeer::MIC_PPS_SER_ID)) $criteria->add(MicrocuencasPeer::MIC_PPS_SER_ID, $this->mic_pps_ser_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);

		$criteria->add(MicrocuencasPeer::MIC_ID, $this->mic_id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getMicId();
	}

	/**
	 * Generic method to set the primary key (mic_id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setMicId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Microcuencas (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setMicPpsPreId($this->mic_pps_pre_id);

		$copyObj->setMicPpsAnio($this->mic_pps_anio);

		$copyObj->setMicPpsSerId($this->mic_pps_ser_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCalidadcantidadaguas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCalidadcantidadagua($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFuentess() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFuentes($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getInformaciongeneralmicrocuencass() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addInformaciongeneralmicrocuencas($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getProteccionfuentessuperficialesaguas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addProteccionfuentessuperficialesagua($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setMicId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Microcuencas Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     MicrocuencasPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new MicrocuencasPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Periodoporprestadorservicio object.
	 *
	 * @param      Periodoporprestadorservicio $v
	 * @return     Microcuencas The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPeriodoporprestadorservicio(Periodoporprestadorservicio $v = null)
	{
		if ($v === null) {
			$this->setMicPpsPreId(NULL);
		} else {
			$this->setMicPpsPreId($v->getPpsPreId());
		}

		$this->aPeriodoporprestadorservicio = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Periodoporprestadorservicio object, it will not be re-added.
		if ($v !== null) {
			$v->addMicrocuencas($this);
		}

		return $this;
	}


	/**
	 * Get the associated Periodoporprestadorservicio object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Periodoporprestadorservicio The associated Periodoporprestadorservicio object.
	 * @throws     PropelException
	 */
	public function getPeriodoporprestadorservicio(PropelPDO $con = null)
	{
		if ($this->aPeriodoporprestadorservicio === null && ($this->mic_pps_pre_id !== null)) {
			$this->aPeriodoporprestadorservicio = PeriodoporprestadorservicioPeer::retrieveByPk($this->mic_pps_pre_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPeriodoporprestadorservicio->addMicrocuencass($this);
			 */
		}
		return $this->aPeriodoporprestadorservicio;
	}

	/**
	 * Clears out the collCalidadcantidadaguas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCalidadcantidadaguas()
	 */
	public function clearCalidadcantidadaguas()
	{
		$this->collCalidadcantidadaguas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCalidadcantidadaguas collection (array).
	 *
	 * By default this just sets the collCalidadcantidadaguas collection to an empty array (like clearcollCalidadcantidadaguas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCalidadcantidadaguas()
	{
		$this->collCalidadcantidadaguas = array();
	}

	/**
	 * Gets an array of Calidadcantidadagua objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Microcuencas has previously been saved, it will retrieve
	 * related Calidadcantidadaguas from storage. If this Microcuencas is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Calidadcantidadagua[]
	 * @throws     PropelException
	 */
	public function getCalidadcantidadaguas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCalidadcantidadaguas === null) {
			if ($this->isNew()) {
			   $this->collCalidadcantidadaguas = array();
			} else {

				$criteria->add(CalidadcantidadaguaPeer::CCA_MIC_ID, $this->mic_id);

				CalidadcantidadaguaPeer::addSelectColumns($criteria);
				$this->collCalidadcantidadaguas = CalidadcantidadaguaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CalidadcantidadaguaPeer::CCA_MIC_ID, $this->mic_id);

				CalidadcantidadaguaPeer::addSelectColumns($criteria);
				if (!isset($this->lastCalidadcantidadaguaCriteria) || !$this->lastCalidadcantidadaguaCriteria->equals($criteria)) {
					$this->collCalidadcantidadaguas = CalidadcantidadaguaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCalidadcantidadaguaCriteria = $criteria;
		return $this->collCalidadcantidadaguas;
	}

	/**
	 * Returns the number of related Calidadcantidadagua objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Calidadcantidadagua objects.
	 * @throws     PropelException
	 */
	public function countCalidadcantidadaguas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCalidadcantidadaguas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CalidadcantidadaguaPeer::CCA_MIC_ID, $this->mic_id);

				$count = CalidadcantidadaguaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CalidadcantidadaguaPeer::CCA_MIC_ID, $this->mic_id);

				if (!isset($this->lastCalidadcantidadaguaCriteria) || !$this->lastCalidadcantidadaguaCriteria->equals($criteria)) {
					$count = CalidadcantidadaguaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCalidadcantidadaguas);
				}
			} else {
				$count = count($this->collCalidadcantidadaguas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Calidadcantidadagua object to this object
	 * through the Calidadcantidadagua foreign key attribute.
	 *
	 * @param      Calidadcantidadagua $l Calidadcantidadagua
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCalidadcantidadagua(Calidadcantidadagua $l)
	{
		if ($this->collCalidadcantidadaguas === null) {
			$this->initCalidadcantidadaguas();
		}
		if (!in_array($l, $this->collCalidadcantidadaguas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCalidadcantidadaguas, $l);
			$l->setMicrocuencas($this);
		}
	}

	/**
	 * Clears out the collFuentess collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFuentess()
	 */
	public function clearFuentess()
	{
		$this->collFuentess = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFuentess collection (array).
	 *
	 * By default this just sets the collFuentess collection to an empty array (like clearcollFuentess());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFuentess()
	{
		$this->collFuentess = array();
	}

	/**
	 * Gets an array of Fuentes objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Microcuencas has previously been saved, it will retrieve
	 * related Fuentess from storage. If this Microcuencas is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Fuentes[]
	 * @throws     PropelException
	 */
	public function getFuentess($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFuentess === null) {
			if ($this->isNew()) {
			   $this->collFuentess = array();
			} else {

				$criteria->add(FuentesPeer::FUE_MIC_ID, $this->mic_id);

				FuentesPeer::addSelectColumns($criteria);
				$this->collFuentess = FuentesPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FuentesPeer::FUE_MIC_ID, $this->mic_id);

				FuentesPeer::addSelectColumns($criteria);
				if (!isset($this->lastFuentesCriteria) || !$this->lastFuentesCriteria->equals($criteria)) {
					$this->collFuentess = FuentesPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFuentesCriteria = $criteria;
		return $this->collFuentess;
	}

	/**
	 * Returns the number of related Fuentes objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Fuentes objects.
	 * @throws     PropelException
	 */
	public function countFuentess(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFuentess === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FuentesPeer::FUE_MIC_ID, $this->mic_id);

				$count = FuentesPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FuentesPeer::FUE_MIC_ID, $this->mic_id);

				if (!isset($this->lastFuentesCriteria) || !$this->lastFuentesCriteria->equals($criteria)) {
					$count = FuentesPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFuentess);
				}
			} else {
				$count = count($this->collFuentess);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Fuentes object to this object
	 * through the Fuentes foreign key attribute.
	 *
	 * @param      Fuentes $l Fuentes
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFuentes(Fuentes $l)
	{
		if ($this->collFuentess === null) {
			$this->initFuentess();
		}
		if (!in_array($l, $this->collFuentess, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFuentess, $l);
			$l->setMicrocuencas($this);
		}
	}

	/**
	 * Clears out the collInformaciongeneralmicrocuencass collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addInformaciongeneralmicrocuencass()
	 */
	public function clearInformaciongeneralmicrocuencass()
	{
		$this->collInformaciongeneralmicrocuencass = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collInformaciongeneralmicrocuencass collection (array).
	 *
	 * By default this just sets the collInformaciongeneralmicrocuencass collection to an empty array (like clearcollInformaciongeneralmicrocuencass());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initInformaciongeneralmicrocuencass()
	{
		$this->collInformaciongeneralmicrocuencass = array();
	}

	/**
	 * Gets an array of Informaciongeneralmicrocuencas objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Microcuencas has previously been saved, it will retrieve
	 * related Informaciongeneralmicrocuencass from storage. If this Microcuencas is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Informaciongeneralmicrocuencas[]
	 * @throws     PropelException
	 */
	public function getInformaciongeneralmicrocuencass($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
			   $this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				InformaciongeneralmicrocuencasPeer::addSelectColumns($criteria);
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				InformaciongeneralmicrocuencasPeer::addSelectColumns($criteria);
				if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
					$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;
		return $this->collInformaciongeneralmicrocuencass;
	}

	/**
	 * Returns the number of related Informaciongeneralmicrocuencas objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Informaciongeneralmicrocuencas objects.
	 * @throws     PropelException
	 */
	public function countInformaciongeneralmicrocuencass(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				$count = InformaciongeneralmicrocuencasPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
					$count = InformaciongeneralmicrocuencasPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collInformaciongeneralmicrocuencass);
				}
			} else {
				$count = count($this->collInformaciongeneralmicrocuencass);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Informaciongeneralmicrocuencas object to this object
	 * through the Informaciongeneralmicrocuencas foreign key attribute.
	 *
	 * @param      Informaciongeneralmicrocuencas $l Informaciongeneralmicrocuencas
	 * @return     void
	 * @throws     PropelException
	 */
	public function addInformaciongeneralmicrocuencas(Informaciongeneralmicrocuencas $l)
	{
		if ($this->collInformaciongeneralmicrocuencass === null) {
			$this->initInformaciongeneralmicrocuencass();
		}
		if (!in_array($l, $this->collInformaciongeneralmicrocuencass, true)) { // only add it if the **same** object is not already associated
			array_push($this->collInformaciongeneralmicrocuencass, $l);
			$l->setMicrocuencas($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Microcuencas is new, it will return
	 * an empty collection; or if this Microcuencas has previously
	 * been saved, it will retrieve related Informaciongeneralmicrocuencass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Microcuencas.
	 */
	public function getInformaciongeneralmicrocuencassJoinDepartamento($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

			if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinDepartamento($criteria, $con, $join_behavior);
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;

		return $this->collInformaciongeneralmicrocuencass;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Microcuencas is new, it will return
	 * an empty collection; or if this Microcuencas has previously
	 * been saved, it will retrieve related Informaciongeneralmicrocuencass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Microcuencas.
	 */
	public function getInformaciongeneralmicrocuencassJoinMunicipio($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformaciongeneralmicrocuencass === null) {
			if ($this->isNew()) {
				$this->collInformaciongeneralmicrocuencass = array();
			} else {

				$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinMunicipio($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(InformaciongeneralmicrocuencasPeer::IMI_MIC_ID, $this->mic_id);

			if (!isset($this->lastInformaciongeneralmicrocuencasCriteria) || !$this->lastInformaciongeneralmicrocuencasCriteria->equals($criteria)) {
				$this->collInformaciongeneralmicrocuencass = InformaciongeneralmicrocuencasPeer::doSelectJoinMunicipio($criteria, $con, $join_behavior);
			}
		}
		$this->lastInformaciongeneralmicrocuencasCriteria = $criteria;

		return $this->collInformaciongeneralmicrocuencass;
	}

	/**
	 * Clears out the collProteccionfuentessuperficialesaguas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addProteccionfuentessuperficialesaguas()
	 */
	public function clearProteccionfuentessuperficialesaguas()
	{
		$this->collProteccionfuentessuperficialesaguas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collProteccionfuentessuperficialesaguas collection (array).
	 *
	 * By default this just sets the collProteccionfuentessuperficialesaguas collection to an empty array (like clearcollProteccionfuentessuperficialesaguas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initProteccionfuentessuperficialesaguas()
	{
		$this->collProteccionfuentessuperficialesaguas = array();
	}

	/**
	 * Gets an array of Proteccionfuentessuperficialesagua objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Microcuencas has previously been saved, it will retrieve
	 * related Proteccionfuentessuperficialesaguas from storage. If this Microcuencas is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Proteccionfuentessuperficialesagua[]
	 * @throws     PropelException
	 */
	public function getProteccionfuentessuperficialesaguas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collProteccionfuentessuperficialesaguas === null) {
			if ($this->isNew()) {
			   $this->collProteccionfuentessuperficialesaguas = array();
			} else {

				$criteria->add(ProteccionfuentessuperficialesaguaPeer::PFU_MIC_ID, $this->mic_id);

				ProteccionfuentessuperficialesaguaPeer::addSelectColumns($criteria);
				$this->collProteccionfuentessuperficialesaguas = ProteccionfuentessuperficialesaguaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ProteccionfuentessuperficialesaguaPeer::PFU_MIC_ID, $this->mic_id);

				ProteccionfuentessuperficialesaguaPeer::addSelectColumns($criteria);
				if (!isset($this->lastProteccionfuentessuperficialesaguaCriteria) || !$this->lastProteccionfuentessuperficialesaguaCriteria->equals($criteria)) {
					$this->collProteccionfuentessuperficialesaguas = ProteccionfuentessuperficialesaguaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastProteccionfuentessuperficialesaguaCriteria = $criteria;
		return $this->collProteccionfuentessuperficialesaguas;
	}

	/**
	 * Returns the number of related Proteccionfuentessuperficialesagua objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Proteccionfuentessuperficialesagua objects.
	 * @throws     PropelException
	 */
	public function countProteccionfuentessuperficialesaguas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MicrocuencasPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collProteccionfuentessuperficialesaguas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ProteccionfuentessuperficialesaguaPeer::PFU_MIC_ID, $this->mic_id);

				$count = ProteccionfuentessuperficialesaguaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ProteccionfuentessuperficialesaguaPeer::PFU_MIC_ID, $this->mic_id);

				if (!isset($this->lastProteccionfuentessuperficialesaguaCriteria) || !$this->lastProteccionfuentessuperficialesaguaCriteria->equals($criteria)) {
					$count = ProteccionfuentessuperficialesaguaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collProteccionfuentessuperficialesaguas);
				}
			} else {
				$count = count($this->collProteccionfuentessuperficialesaguas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Proteccionfuentessuperficialesagua object to this object
	 * through the Proteccionfuentessuperficialesagua foreign key attribute.
	 *
	 * @param      Proteccionfuentessuperficialesagua $l Proteccionfuentessuperficialesagua
	 * @return     void
	 * @throws     PropelException
	 */
	public function addProteccionfuentessuperficialesagua(Proteccionfuentessuperficialesagua $l)
	{
		if ($this->collProteccionfuentessuperficialesaguas === null) {
			$this->initProteccionfuentessuperficialesaguas();
		}
		if (!in_array($l, $this->collProteccionfuentessuperficialesaguas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collProteccionfuentessuperficialesaguas, $l);
			$l->setMicrocuencas($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCalidadcantidadaguas) {
				foreach ((array) $this->collCalidadcantidadaguas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFuentess) {
				foreach ((array) $this->collFuentess as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collInformaciongeneralmicrocuencass) {
				foreach ((array) $this->collInformaciongeneralmicrocuencass as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collProteccionfuentessuperficialesaguas) {
				foreach ((array) $this->collProteccionfuentessuperficialesaguas as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCalidadcantidadaguas = null;
		$this->collFuentess = null;
		$this->collInformaciongeneralmicrocuencass = null;
		$this->collProteccionfuentessuperficialesaguas = null;
			$this->aPeriodoporprestadorservicio = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseMicrocuencas:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseMicrocuencas::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseMicrocuencas
