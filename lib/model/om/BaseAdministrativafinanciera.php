<?php

/**
 * Base class that represents a row from the 'administrativafinanciera' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * 07/27/10 18:47:59
 *
 * @package    lib.model.om
 */
abstract class BaseAdministrativafinanciera extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        AdministrativafinancieraPeer
	 */
	protected static $peer;

	/**
	 * The value for the iaf_id field.
	 * @var        int
	 */
	protected $iaf_id;

	/**
	 * The value for the iaf_pps_pre_id field.
	 * @var        int
	 */
	protected $iaf_pps_pre_id;

	/**
	 * The value for the iaf_pps_anio field.
	 * @var        int
	 */
	protected $iaf_pps_anio;

	/**
	 * The value for the iaf_pps_ser_id field.
	 * @var        int
	 */
	protected $iaf_pps_ser_id;

	/**
	 * @var        Periodoporprestadorservicio
	 */
	protected $aPeriodoporprestadorservicio;

	/**
	 * @var        array Capacitacion[] Collection to store aggregation of Capacitacion objects.
	 */
	protected $collCapacitacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCapacitacions.
	 */
	private $lastCapacitacionCriteria = null;

	/**
	 * @var        array Gestionresultado[] Collection to store aggregation of Gestionresultado objects.
	 */
	protected $collGestionresultados;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGestionresultados.
	 */
	private $lastGestionresultadoCriteria = null;

	/**
	 * @var        array Informacioncontable[] Collection to store aggregation of Informacioncontable objects.
	 */
	protected $collInformacioncontables;

	/**
	 * @var        Criteria The criteria used to select the current contents of collInformacioncontables.
	 */
	private $lastInformacioncontableCriteria = null;

	/**
	 * @var        array Participacionciudadana[] Collection to store aggregation of Participacionciudadana objects.
	 */
	protected $collParticipacionciudadanas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collParticipacionciudadanas.
	 */
	private $lastParticipacionciudadanaCriteria = null;

	/**
	 * @var        array Trabajadoresyvinculacion[] Collection to store aggregation of Trabajadoresyvinculacion objects.
	 */
	protected $collTrabajadoresyvinculacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTrabajadoresyvinculacions.
	 */
	private $lastTrabajadoresyvinculacionCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'AdministrativafinancieraPeer';

	/**
	 * Get the [iaf_id] column value.
	 * 
	 * @return     int
	 */
	public function getIafId()
	{
		return $this->iaf_id;
	}

	/**
	 * Get the [iaf_pps_pre_id] column value.
	 * 
	 * @return     int
	 */
	public function getIafPpsPreId()
	{
		return $this->iaf_pps_pre_id;
	}

	/**
	 * Get the [iaf_pps_anio] column value.
	 * 
	 * @return     int
	 */
	public function getIafPpsAnio()
	{
		return $this->iaf_pps_anio;
	}

	/**
	 * Get the [iaf_pps_ser_id] column value.
	 * 
	 * @return     int
	 */
	public function getIafPpsSerId()
	{
		return $this->iaf_pps_ser_id;
	}

	/**
	 * Set the value of [iaf_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Administrativafinanciera The current object (for fluent API support)
	 */
	public function setIafId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->iaf_id !== $v) {
			$this->iaf_id = $v;
			$this->modifiedColumns[] = AdministrativafinancieraPeer::IAF_ID;
		}

		return $this;
	} // setIafId()

	/**
	 * Set the value of [iaf_pps_pre_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Administrativafinanciera The current object (for fluent API support)
	 */
	public function setIafPpsPreId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->iaf_pps_pre_id !== $v) {
			$this->iaf_pps_pre_id = $v;
			$this->modifiedColumns[] = AdministrativafinancieraPeer::IAF_PPS_PRE_ID;
		}

		if ($this->aPeriodoporprestadorservicio !== null && $this->aPeriodoporprestadorservicio->getPpsPreId() !== $v) {
			$this->aPeriodoporprestadorservicio = null;
		}

		return $this;
	} // setIafPpsPreId()

	/**
	 * Set the value of [iaf_pps_anio] column.
	 * 
	 * @param      int $v new value
	 * @return     Administrativafinanciera The current object (for fluent API support)
	 */
	public function setIafPpsAnio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->iaf_pps_anio !== $v) {
			$this->iaf_pps_anio = $v;
			$this->modifiedColumns[] = AdministrativafinancieraPeer::IAF_PPS_ANIO;
		}

		return $this;
	} // setIafPpsAnio()

	/**
	 * Set the value of [iaf_pps_ser_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Administrativafinanciera The current object (for fluent API support)
	 */
	public function setIafPpsSerId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->iaf_pps_ser_id !== $v) {
			$this->iaf_pps_ser_id = $v;
			$this->modifiedColumns[] = AdministrativafinancieraPeer::IAF_PPS_SER_ID;
		}

		return $this;
	} // setIafPpsSerId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->iaf_id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->iaf_pps_pre_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->iaf_pps_anio = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->iaf_pps_ser_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 4; // 4 = AdministrativafinancieraPeer::NUM_COLUMNS - AdministrativafinancieraPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Administrativafinanciera object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aPeriodoporprestadorservicio !== null && $this->iaf_pps_pre_id !== $this->aPeriodoporprestadorservicio->getPpsPreId()) {
			$this->aPeriodoporprestadorservicio = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AdministrativafinancieraPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = AdministrativafinancieraPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aPeriodoporprestadorservicio = null;
			$this->collCapacitacions = null;
			$this->lastCapacitacionCriteria = null;

			$this->collGestionresultados = null;
			$this->lastGestionresultadoCriteria = null;

			$this->collInformacioncontables = null;
			$this->lastInformacioncontableCriteria = null;

			$this->collParticipacionciudadanas = null;
			$this->lastParticipacionciudadanaCriteria = null;

			$this->collTrabajadoresyvinculacions = null;
			$this->lastTrabajadoresyvinculacionCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AdministrativafinancieraPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAdministrativafinanciera:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				AdministrativafinancieraPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAdministrativafinanciera:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AdministrativafinancieraPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAdministrativafinanciera:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAdministrativafinanciera:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				AdministrativafinancieraPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPeriodoporprestadorservicio !== null) {
				if ($this->aPeriodoporprestadorservicio->isModified() || $this->aPeriodoporprestadorservicio->isNew()) {
					$affectedRows += $this->aPeriodoporprestadorservicio->save($con);
				}
				$this->setPeriodoporprestadorservicio($this->aPeriodoporprestadorservicio);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = AdministrativafinancieraPeer::IAF_ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = AdministrativafinancieraPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setIafId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += AdministrativafinancieraPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCapacitacions !== null) {
				foreach ($this->collCapacitacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collGestionresultados !== null) {
				foreach ($this->collGestionresultados as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collInformacioncontables !== null) {
				foreach ($this->collInformacioncontables as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collParticipacionciudadanas !== null) {
				foreach ($this->collParticipacionciudadanas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTrabajadoresyvinculacions !== null) {
				foreach ($this->collTrabajadoresyvinculacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aPeriodoporprestadorservicio !== null) {
				if (!$this->aPeriodoporprestadorservicio->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPeriodoporprestadorservicio->getValidationFailures());
				}
			}


			if (($retval = AdministrativafinancieraPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCapacitacions !== null) {
					foreach ($this->collCapacitacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collGestionresultados !== null) {
					foreach ($this->collGestionresultados as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collInformacioncontables !== null) {
					foreach ($this->collInformacioncontables as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collParticipacionciudadanas !== null) {
					foreach ($this->collParticipacionciudadanas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTrabajadoresyvinculacions !== null) {
					foreach ($this->collTrabajadoresyvinculacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AdministrativafinancieraPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIafId();
				break;
			case 1:
				return $this->getIafPpsPreId();
				break;
			case 2:
				return $this->getIafPpsAnio();
				break;
			case 3:
				return $this->getIafPpsSerId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = AdministrativafinancieraPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getIafId(),
			$keys[1] => $this->getIafPpsPreId(),
			$keys[2] => $this->getIafPpsAnio(),
			$keys[3] => $this->getIafPpsSerId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AdministrativafinancieraPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIafId($value);
				break;
			case 1:
				$this->setIafPpsPreId($value);
				break;
			case 2:
				$this->setIafPpsAnio($value);
				break;
			case 3:
				$this->setIafPpsSerId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = AdministrativafinancieraPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIafId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIafPpsPreId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setIafPpsAnio($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setIafPpsSerId($arr[$keys[3]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);

		if ($this->isColumnModified(AdministrativafinancieraPeer::IAF_ID)) $criteria->add(AdministrativafinancieraPeer::IAF_ID, $this->iaf_id);
		if ($this->isColumnModified(AdministrativafinancieraPeer::IAF_PPS_PRE_ID)) $criteria->add(AdministrativafinancieraPeer::IAF_PPS_PRE_ID, $this->iaf_pps_pre_id);
		if ($this->isColumnModified(AdministrativafinancieraPeer::IAF_PPS_ANIO)) $criteria->add(AdministrativafinancieraPeer::IAF_PPS_ANIO, $this->iaf_pps_anio);
		if ($this->isColumnModified(AdministrativafinancieraPeer::IAF_PPS_SER_ID)) $criteria->add(AdministrativafinancieraPeer::IAF_PPS_SER_ID, $this->iaf_pps_ser_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);

		$criteria->add(AdministrativafinancieraPeer::IAF_ID, $this->iaf_id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getIafId();
	}

	/**
	 * Generic method to set the primary key (iaf_id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setIafId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Administrativafinanciera (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIafPpsPreId($this->iaf_pps_pre_id);

		$copyObj->setIafPpsAnio($this->iaf_pps_anio);

		$copyObj->setIafPpsSerId($this->iaf_pps_ser_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCapacitacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCapacitacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getGestionresultados() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGestionresultado($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getInformacioncontables() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addInformacioncontable($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getParticipacionciudadanas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addParticipacionciudadana($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTrabajadoresyvinculacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTrabajadoresyvinculacion($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setIafId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Administrativafinanciera Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     AdministrativafinancieraPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new AdministrativafinancieraPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Periodoporprestadorservicio object.
	 *
	 * @param      Periodoporprestadorservicio $v
	 * @return     Administrativafinanciera The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPeriodoporprestadorservicio(Periodoporprestadorservicio $v = null)
	{
		if ($v === null) {
			$this->setIafPpsPreId(NULL);
		} else {
			$this->setIafPpsPreId($v->getPpsPreId());
		}

		$this->aPeriodoporprestadorservicio = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Periodoporprestadorservicio object, it will not be re-added.
		if ($v !== null) {
			$v->addAdministrativafinanciera($this);
		}

		return $this;
	}


	/**
	 * Get the associated Periodoporprestadorservicio object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Periodoporprestadorservicio The associated Periodoporprestadorservicio object.
	 * @throws     PropelException
	 */
	public function getPeriodoporprestadorservicio(PropelPDO $con = null)
	{
		if ($this->aPeriodoporprestadorservicio === null && ($this->iaf_pps_pre_id !== null)) {
			$this->aPeriodoporprestadorservicio = PeriodoporprestadorservicioPeer::retrieveByPk($this->iaf_pps_pre_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPeriodoporprestadorservicio->addAdministrativafinancieras($this);
			 */
		}
		return $this->aPeriodoporprestadorservicio;
	}

	/**
	 * Clears out the collCapacitacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCapacitacions()
	 */
	public function clearCapacitacions()
	{
		$this->collCapacitacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCapacitacions collection (array).
	 *
	 * By default this just sets the collCapacitacions collection to an empty array (like clearcollCapacitacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCapacitacions()
	{
		$this->collCapacitacions = array();
	}

	/**
	 * Gets an array of Capacitacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Administrativafinanciera has previously been saved, it will retrieve
	 * related Capacitacions from storage. If this Administrativafinanciera is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Capacitacion[]
	 * @throws     PropelException
	 */
	public function getCapacitacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCapacitacions === null) {
			if ($this->isNew()) {
			   $this->collCapacitacions = array();
			} else {

				$criteria->add(CapacitacionPeer::CAP_IAF_ID, $this->iaf_id);

				CapacitacionPeer::addSelectColumns($criteria);
				$this->collCapacitacions = CapacitacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CapacitacionPeer::CAP_IAF_ID, $this->iaf_id);

				CapacitacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastCapacitacionCriteria) || !$this->lastCapacitacionCriteria->equals($criteria)) {
					$this->collCapacitacions = CapacitacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCapacitacionCriteria = $criteria;
		return $this->collCapacitacions;
	}

	/**
	 * Returns the number of related Capacitacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Capacitacion objects.
	 * @throws     PropelException
	 */
	public function countCapacitacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCapacitacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CapacitacionPeer::CAP_IAF_ID, $this->iaf_id);

				$count = CapacitacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CapacitacionPeer::CAP_IAF_ID, $this->iaf_id);

				if (!isset($this->lastCapacitacionCriteria) || !$this->lastCapacitacionCriteria->equals($criteria)) {
					$count = CapacitacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCapacitacions);
				}
			} else {
				$count = count($this->collCapacitacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Capacitacion object to this object
	 * through the Capacitacion foreign key attribute.
	 *
	 * @param      Capacitacion $l Capacitacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCapacitacion(Capacitacion $l)
	{
		if ($this->collCapacitacions === null) {
			$this->initCapacitacions();
		}
		if (!in_array($l, $this->collCapacitacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCapacitacions, $l);
			$l->setAdministrativafinanciera($this);
		}
	}

	/**
	 * Clears out the collGestionresultados collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGestionresultados()
	 */
	public function clearGestionresultados()
	{
		$this->collGestionresultados = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGestionresultados collection (array).
	 *
	 * By default this just sets the collGestionresultados collection to an empty array (like clearcollGestionresultados());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGestionresultados()
	{
		$this->collGestionresultados = array();
	}

	/**
	 * Gets an array of Gestionresultado objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Administrativafinanciera has previously been saved, it will retrieve
	 * related Gestionresultados from storage. If this Administrativafinanciera is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Gestionresultado[]
	 * @throws     PropelException
	 */
	public function getGestionresultados($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGestionresultados === null) {
			if ($this->isNew()) {
			   $this->collGestionresultados = array();
			} else {

				$criteria->add(GestionresultadoPeer::GRE_IAF_ID, $this->iaf_id);

				GestionresultadoPeer::addSelectColumns($criteria);
				$this->collGestionresultados = GestionresultadoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GestionresultadoPeer::GRE_IAF_ID, $this->iaf_id);

				GestionresultadoPeer::addSelectColumns($criteria);
				if (!isset($this->lastGestionresultadoCriteria) || !$this->lastGestionresultadoCriteria->equals($criteria)) {
					$this->collGestionresultados = GestionresultadoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGestionresultadoCriteria = $criteria;
		return $this->collGestionresultados;
	}

	/**
	 * Returns the number of related Gestionresultado objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Gestionresultado objects.
	 * @throws     PropelException
	 */
	public function countGestionresultados(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGestionresultados === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GestionresultadoPeer::GRE_IAF_ID, $this->iaf_id);

				$count = GestionresultadoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GestionresultadoPeer::GRE_IAF_ID, $this->iaf_id);

				if (!isset($this->lastGestionresultadoCriteria) || !$this->lastGestionresultadoCriteria->equals($criteria)) {
					$count = GestionresultadoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGestionresultados);
				}
			} else {
				$count = count($this->collGestionresultados);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Gestionresultado object to this object
	 * through the Gestionresultado foreign key attribute.
	 *
	 * @param      Gestionresultado $l Gestionresultado
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGestionresultado(Gestionresultado $l)
	{
		if ($this->collGestionresultados === null) {
			$this->initGestionresultados();
		}
		if (!in_array($l, $this->collGestionresultados, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGestionresultados, $l);
			$l->setAdministrativafinanciera($this);
		}
	}

	/**
	 * Clears out the collInformacioncontables collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addInformacioncontables()
	 */
	public function clearInformacioncontables()
	{
		$this->collInformacioncontables = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collInformacioncontables collection (array).
	 *
	 * By default this just sets the collInformacioncontables collection to an empty array (like clearcollInformacioncontables());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initInformacioncontables()
	{
		$this->collInformacioncontables = array();
	}

	/**
	 * Gets an array of Informacioncontable objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Administrativafinanciera has previously been saved, it will retrieve
	 * related Informacioncontables from storage. If this Administrativafinanciera is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Informacioncontable[]
	 * @throws     PropelException
	 */
	public function getInformacioncontables($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collInformacioncontables === null) {
			if ($this->isNew()) {
			   $this->collInformacioncontables = array();
			} else {

				$criteria->add(InformacioncontablePeer::ICO_IAF_ID, $this->iaf_id);

				InformacioncontablePeer::addSelectColumns($criteria);
				$this->collInformacioncontables = InformacioncontablePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(InformacioncontablePeer::ICO_IAF_ID, $this->iaf_id);

				InformacioncontablePeer::addSelectColumns($criteria);
				if (!isset($this->lastInformacioncontableCriteria) || !$this->lastInformacioncontableCriteria->equals($criteria)) {
					$this->collInformacioncontables = InformacioncontablePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastInformacioncontableCriteria = $criteria;
		return $this->collInformacioncontables;
	}

	/**
	 * Returns the number of related Informacioncontable objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Informacioncontable objects.
	 * @throws     PropelException
	 */
	public function countInformacioncontables(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collInformacioncontables === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(InformacioncontablePeer::ICO_IAF_ID, $this->iaf_id);

				$count = InformacioncontablePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(InformacioncontablePeer::ICO_IAF_ID, $this->iaf_id);

				if (!isset($this->lastInformacioncontableCriteria) || !$this->lastInformacioncontableCriteria->equals($criteria)) {
					$count = InformacioncontablePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collInformacioncontables);
				}
			} else {
				$count = count($this->collInformacioncontables);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Informacioncontable object to this object
	 * through the Informacioncontable foreign key attribute.
	 *
	 * @param      Informacioncontable $l Informacioncontable
	 * @return     void
	 * @throws     PropelException
	 */
	public function addInformacioncontable(Informacioncontable $l)
	{
		if ($this->collInformacioncontables === null) {
			$this->initInformacioncontables();
		}
		if (!in_array($l, $this->collInformacioncontables, true)) { // only add it if the **same** object is not already associated
			array_push($this->collInformacioncontables, $l);
			$l->setAdministrativafinanciera($this);
		}
	}

	/**
	 * Clears out the collParticipacionciudadanas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addParticipacionciudadanas()
	 */
	public function clearParticipacionciudadanas()
	{
		$this->collParticipacionciudadanas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collParticipacionciudadanas collection (array).
	 *
	 * By default this just sets the collParticipacionciudadanas collection to an empty array (like clearcollParticipacionciudadanas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initParticipacionciudadanas()
	{
		$this->collParticipacionciudadanas = array();
	}

	/**
	 * Gets an array of Participacionciudadana objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Administrativafinanciera has previously been saved, it will retrieve
	 * related Participacionciudadanas from storage. If this Administrativafinanciera is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Participacionciudadana[]
	 * @throws     PropelException
	 */
	public function getParticipacionciudadanas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collParticipacionciudadanas === null) {
			if ($this->isNew()) {
			   $this->collParticipacionciudadanas = array();
			} else {

				$criteria->add(ParticipacionciudadanaPeer::PAR_IAF_ID, $this->iaf_id);

				ParticipacionciudadanaPeer::addSelectColumns($criteria);
				$this->collParticipacionciudadanas = ParticipacionciudadanaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ParticipacionciudadanaPeer::PAR_IAF_ID, $this->iaf_id);

				ParticipacionciudadanaPeer::addSelectColumns($criteria);
				if (!isset($this->lastParticipacionciudadanaCriteria) || !$this->lastParticipacionciudadanaCriteria->equals($criteria)) {
					$this->collParticipacionciudadanas = ParticipacionciudadanaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastParticipacionciudadanaCriteria = $criteria;
		return $this->collParticipacionciudadanas;
	}

	/**
	 * Returns the number of related Participacionciudadana objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Participacionciudadana objects.
	 * @throws     PropelException
	 */
	public function countParticipacionciudadanas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collParticipacionciudadanas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ParticipacionciudadanaPeer::PAR_IAF_ID, $this->iaf_id);

				$count = ParticipacionciudadanaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ParticipacionciudadanaPeer::PAR_IAF_ID, $this->iaf_id);

				if (!isset($this->lastParticipacionciudadanaCriteria) || !$this->lastParticipacionciudadanaCriteria->equals($criteria)) {
					$count = ParticipacionciudadanaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collParticipacionciudadanas);
				}
			} else {
				$count = count($this->collParticipacionciudadanas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Participacionciudadana object to this object
	 * through the Participacionciudadana foreign key attribute.
	 *
	 * @param      Participacionciudadana $l Participacionciudadana
	 * @return     void
	 * @throws     PropelException
	 */
	public function addParticipacionciudadana(Participacionciudadana $l)
	{
		if ($this->collParticipacionciudadanas === null) {
			$this->initParticipacionciudadanas();
		}
		if (!in_array($l, $this->collParticipacionciudadanas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collParticipacionciudadanas, $l);
			$l->setAdministrativafinanciera($this);
		}
	}

	/**
	 * Clears out the collTrabajadoresyvinculacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTrabajadoresyvinculacions()
	 */
	public function clearTrabajadoresyvinculacions()
	{
		$this->collTrabajadoresyvinculacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTrabajadoresyvinculacions collection (array).
	 *
	 * By default this just sets the collTrabajadoresyvinculacions collection to an empty array (like clearcollTrabajadoresyvinculacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTrabajadoresyvinculacions()
	{
		$this->collTrabajadoresyvinculacions = array();
	}

	/**
	 * Gets an array of Trabajadoresyvinculacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Administrativafinanciera has previously been saved, it will retrieve
	 * related Trabajadoresyvinculacions from storage. If this Administrativafinanciera is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Trabajadoresyvinculacion[]
	 * @throws     PropelException
	 */
	public function getTrabajadoresyvinculacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajadoresyvinculacions === null) {
			if ($this->isNew()) {
			   $this->collTrabajadoresyvinculacions = array();
			} else {

				$criteria->add(TrabajadoresyvinculacionPeer::TRA_IAF_ID, $this->iaf_id);

				TrabajadoresyvinculacionPeer::addSelectColumns($criteria);
				$this->collTrabajadoresyvinculacions = TrabajadoresyvinculacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TrabajadoresyvinculacionPeer::TRA_IAF_ID, $this->iaf_id);

				TrabajadoresyvinculacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastTrabajadoresyvinculacionCriteria) || !$this->lastTrabajadoresyvinculacionCriteria->equals($criteria)) {
					$this->collTrabajadoresyvinculacions = TrabajadoresyvinculacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTrabajadoresyvinculacionCriteria = $criteria;
		return $this->collTrabajadoresyvinculacions;
	}

	/**
	 * Returns the number of related Trabajadoresyvinculacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Trabajadoresyvinculacion objects.
	 * @throws     PropelException
	 */
	public function countTrabajadoresyvinculacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(AdministrativafinancieraPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTrabajadoresyvinculacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TrabajadoresyvinculacionPeer::TRA_IAF_ID, $this->iaf_id);

				$count = TrabajadoresyvinculacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TrabajadoresyvinculacionPeer::TRA_IAF_ID, $this->iaf_id);

				if (!isset($this->lastTrabajadoresyvinculacionCriteria) || !$this->lastTrabajadoresyvinculacionCriteria->equals($criteria)) {
					$count = TrabajadoresyvinculacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTrabajadoresyvinculacions);
				}
			} else {
				$count = count($this->collTrabajadoresyvinculacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Trabajadoresyvinculacion object to this object
	 * through the Trabajadoresyvinculacion foreign key attribute.
	 *
	 * @param      Trabajadoresyvinculacion $l Trabajadoresyvinculacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTrabajadoresyvinculacion(Trabajadoresyvinculacion $l)
	{
		if ($this->collTrabajadoresyvinculacions === null) {
			$this->initTrabajadoresyvinculacions();
		}
		if (!in_array($l, $this->collTrabajadoresyvinculacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTrabajadoresyvinculacions, $l);
			$l->setAdministrativafinanciera($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCapacitacions) {
				foreach ((array) $this->collCapacitacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collGestionresultados) {
				foreach ((array) $this->collGestionresultados as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collInformacioncontables) {
				foreach ((array) $this->collInformacioncontables as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collParticipacionciudadanas) {
				foreach ((array) $this->collParticipacionciudadanas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTrabajadoresyvinculacions) {
				foreach ((array) $this->collTrabajadoresyvinculacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCapacitacions = null;
		$this->collGestionresultados = null;
		$this->collInformacioncontables = null;
		$this->collParticipacionciudadanas = null;
		$this->collTrabajadoresyvinculacions = null;
			$this->aPeriodoporprestadorservicio = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseAdministrativafinanciera:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseAdministrativafinanciera::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseAdministrativafinanciera
